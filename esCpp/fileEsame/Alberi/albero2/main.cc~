#include <iostream>
#include <fstream>
#include "albero.h"

using namespace std;



int main() {
  fstream input;
  input.open("input.txt", ios::in);
  tree newTree;
  init(newTree);
  
  
  input.close();
  return 0;
}

